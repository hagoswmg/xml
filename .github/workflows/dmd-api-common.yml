name: DMD-API-COMMON CI/CD Pipeline Testing 
on:
  push:
    branches:
      - main

jobs:
  build:

    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK
        uses: actions/setup-java@v1
        with:
          java-version: '11'
      - name: Cache Maven packages
        uses: actions/cache@v1
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2
      - name: Build with Maven
        run: mvn -B package -DskipTests --file pom.xml
      - name: Upload JAR
        uses: actions/upload-artifact@v2
        with:
          name: artifact
          path: target/dmdapicommon-0.0.1-SNAPSHOT.jar

  # deploy:
  #   needs: build
  #   name: Deploy
  #   runs-on: ubuntu-latest
  #   steps:
  #     - name: Download JAR
  #       uses: actions/download-artifact@v2
  #       with:
  #         name: artifact
  #     - name: Deploy to EB
  #       uses: einaregilsson/beanstalk-deploy@v13
  #       with:
  #         aws_access_key: ${{ secrets.AWS_ACCESS_KEY_ID }}
  #         aws_secret_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  #         region: ${{ secrets.AWS_REGION }}
  #         use_existing_version_if_available: false
  #         application_name: DMD-API-COMMON
  #         environment_name: Dmdapicommon-env
  #         version_label: ${{github.SHA}}
  #         deployment_package: dmdapicommon-0.0.1-SNAPSHOT.jar

  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ secrets.ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.SECRET_ACCESS_KEY }}
          aws-region: "us-west-1"

      - name: Upload package to S3 bucket
        run: aws s3 cp dmdapicommon-0.0.1-SNAPSHOT.jar s3://xml-deployment-s3-bucket/

      - name: Create new ElasticBeanstalk Application Version
        run: |
          aws elasticbeanstalk create-application-version \
          --application-name XmlApp \
          --source-bundle S3Bucket="xml-deployment-s3-bucket",S3Key="dmdapicommon-0.0.1-SNAPSHOT.jar" \
          --version-label "ver-${{ github.sha }}" \
          --description "commit-sha-${{ github.sha }}"

      - name: Deploy new ElasticBeanstalk Application Version
        run: aws elasticbeanstalk update-environment --environment-name XmlApp-env-1 --version-label "ver-${{ github.sha }}"